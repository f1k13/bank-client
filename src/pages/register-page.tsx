import { AuthLayout } from "@/app/layouts";
import { Register } from "@/widgets/register";

const RegisterPage = () => {
  return (
    <AuthLayout>
      <Register />
    </AuthLayout>
  );
};

export default RegisterPage;
