import { MainWidgets } from "@/widgets/main-widgets";

const HomePage = () => {
  return (
    <div className="w-full h-full pl-[20px]">
      <MainWidgets />
    </div>
  );
};

export default HomePage;
