import { AuthLayout } from "@/app/layouts";
import { Login } from "@/widgets/login";

const LoginPage = () => {
  return (
    <AuthLayout>
      <Login />
    </AuthLayout>
  );
};

export default LoginPage;
